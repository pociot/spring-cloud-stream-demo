This project will contain some snippets for spring-cloud-stream.

org.springframework.cloud.stream.config.BindingServiceProperties.getBindingProperties - if no properties destination same as channel name.